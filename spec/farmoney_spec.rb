# frozen_string_literal: true

RSpec.describe Farmoney do
  it "has a version number" do
    expect(Farmoney::VERSION).not_to be nil
  end

  it "doesn't do anything useful" do
    expect(true).to eq(true)
  end
end
